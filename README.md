# TrueVal React Frontend

This is the frontend for the TrueVal AI property valuation app. It connects to a Flask backend for real-time property price predictions.

## 🚀 Start Locally

1. Install dependencies:
   ```bash
   npm install
